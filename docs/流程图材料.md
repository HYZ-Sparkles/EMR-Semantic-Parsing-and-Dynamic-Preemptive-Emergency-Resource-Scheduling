逻辑结构示意图
graph TD
    %% ===== 1. 前端表现层 =====
    subgraph FE["前端表现层"]
        SP[患者信息输入\n线段树分析危险分数]  
        QD[大顶堆实时看板]
        RG[资源占用情况显示\n绿/黄/红三色]
        CP[软件控制台\n启动仿真、清空]
        EL[事件日志表]
        RT[危险分参考表]
        
    end  
    %% ===== 2. 接入层 =====
    subgraph AC["接入层"]
      subgraph PO[POST]
        R1["/api/parse\n计算危险分数"]
        R2["/api/admit\n患者登记"]
        R3["/api/random_admit\n随机患者模拟"]
        R5["/api/tick\n手动推进时间"]
        R6["/api/auto\n开启自动仿真"]
        R7["/api/reset\n清空"]
      end
      subgraph GET[GET]
        R4["/api/snapshot\n获取数据快照"]
        R8["/api/terms\n获取医疗词汇"]
        WS["/ws\nWebSocket 升级<br/>subscribe broadcast::Receiver"]
      end
        
      SAB["step_and_broadcast\n统一推进时间，发送信息"]
    end

    %% ===== 3. 仿真调度核心层 =====
    subgraph SC["仿真调度核心层"]
      subgraph SCT3[分配医疗资源]
        SCT31[尝试直接分配空闲资源]-->SCT32[资源抢占判断\n堆顶患者危险分严格大于所需资源中最低分数患者]
      end
      subgraph SCT2[仿真时间步]
        SCT21[调度器时间+1\n接收到来的预约患者]-->SCT23[在等待的患者恶化]-->SCT22[推进在治疗的进度]-->SCT24[挂起列表恢复和重新分配医疗资源]
      end
      subgraph SCT1[接收新患者]
        SCT11[患者条件判断]
        SCT11-->SCT12[预约]-->SCT13[加入预约队列\n跟随仿真时间步]
        SCT11-->SCT14[此刻到来]-->SCT15[记录信息]-->SCT16[分配医疗资源]
      end
      SCT4[生成事件方便前端展示]
    end

    %% ===== 4. 数据结构基础层 =====
    subgraph DS["数据结构基础层"]
        T1["枚举<br/>TERMS: 19 个医学词"]
        T2["线段树\n计算危险分数"]
        T3["大顶堆\n看板排队，O(log(n))"]
        T4["医疗资源状态表\n不同种类的医疗资源和抢占挂起列表"]
    end
    DS-->|基础|SC
    SC<-->|后端通信|AC
    AC<-->|前端通信|FE

---
物理结构示意图
graph LR
    subgraph DISK["磁盘文件系统"]
        direction TB
        subgraph BE_DIR["backend/ (Rust)"]
            CARGO["Cargo.toml\n各种第三方库"]
            MAIN["src/main.rs\n后端入口"]
            TERM["src/term.rs\n医疗词汇"]
            TRIE["src/trie.rs\n线段树"]
            HEAP["src/heap.rs\n大顶堆"]
            RES["src/resources.rs\n医疗资源状态表"]
            SCHD["src/scheduler.rs\n调度器"]
            APIDIR["src/api.rs\n通信api"]
        end
        subgraph FE_DIR["frontend/ (Vue 3 + Vite)"]
            PKG["package.json\n开发配置和依赖"]
            VITE["vite.config.js\n指定前后端服务器通信地址\n"]
            IDX["index.html (#app)"]
            MAINJS["src/main.js<br/>createApp、ElementPlus"]
            STYLE["src/styles.css\n全局CSS样式"]
            APIDIR2["src/api.js\n前端接口配置"]
            UTIL["src/utils/kinds.js\n医疗资源枚举映射"]
            APP["src/App.vue\n前端框架"]
            COMPS["src/components/\n前端展示划分的模块：\nSemanticPanel、QueueDashboard、ResourceGrid、ControlPanel、EventLog、ReferenceTable"]
        end
        DOCS["docs/<br/>个人学习笔记"]
    end

    subgraph RUN["运行时进程"]
        direction TB
        BE_PROC["后端进程<br/>tokio runtime (单进程)<br/>任务 1: Axum HTTP+WS<br/>任务 2: spawn_tick_loop 自动步进"]
        SHARED["Arc&lt;RwLock&lt;Scheduler&gt;&gt;<br/>(进程内共享状态)"]
        BROADCAST["tokio::sync::broadcast::Sender&lt;SnapshotMsg&gt"]
        TICKER["tick 循环 tokio task<br/>200ms 轮询 auto_running<br/>按 tick_ms 步进"]
        FE_PROC["前端 Vite Dev Server<br/>node 进程 :5173<br/>代理 + HMR"]
        BROWSER["浏览器 (任意数量)<br/>SPA 单页应用"]
    end

    subgraph NET["网络位置"]
        BEPORT["0.0.0.0:7878<br/>HTTP/1.1 + WebSocket"]
        FEPORT["0.0.0.0:5173<br/>HTTP/1.1\n(静态资源 + 代理)"]
    end
配合powerpoint完成完整版流程图